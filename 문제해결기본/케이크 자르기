#include <stdio.h>

int main()
{
    int a1, a2, b1, b2, temp;
    scanf("%d %d", &a1, &a2);
    scanf("%d %d", &b1, &b2);
    
    if(a1>a2){
        temp=a1;
        a1=a2;
        a2=temp;
    }
    
    if(b1>b2){
        temp=b1;
        b1=b2;
        b2=temp;
    }
    
    if(b1>a2){
        if(b2>a1 || b2<a2)
            printf("not cross\n");
        else
            printf("cross\n");
    }
    
    if(b1>a1 && b1<a2){
        if(b2>a1 && b2<a2)
            printf("not cross\n");
        else
            printf("cross\n");
    }
    
    if(b1<a1){
        if(b2>a2 || b2<a1)
            printf("not cross\n");
        else
            printf("cross\n");
    }
    return 0;
}
