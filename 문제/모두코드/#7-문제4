1000000 이하의 피보나치 수열 ( N 번째 항이 N - 1 번째 항과 N - 2 번째 항으로 표현되는 수열, 시작은 1,1,2,3,5,8,...) 의 짝수 항들의 합을 구한다.
#include <stdio.h>
int main(){
    int arr[100000];
    arr[0]=1, arr[1]=1;
    int i,sum=0, cnt=0;
    for(i=2; ; i++){
        arr[i]=arr[i-1]+arr[i-2];   // 피보나치 수열의 값을 배열의 요소에 하나씩 집어넣는다
        if(arr[i]>100000){          // 만약 피보나치 수열의 값이 100,000을 넘어가면
            cnt = i;                // cnt에 i를 저장하고(수열의 항이 몇 개인지 세기 위함), 반복문을 탈출한다.
            break;
        }
    }
    for(i=0; i<cnt; i++){          // 배열에 피보나치 수열의 값들을 저장했으므로, 수열의 항 만큼 반복문을 돌리고
        if(i%2!=0)                 // 피보나치 수열의 짝수 값들만 더해준다
            sum+=arr[i];
    }
    printf("%d", sum);
}
