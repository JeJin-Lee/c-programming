#include <stdio.h>

int mul(int n){
    if(n==1)
    return 1;
    
    return n * mul(n-1);
}
int main()
{
    int num, result;
    scanf("%d", &num);
    result = mul(num);
    
    printf("%d", result);

}


* 간단한 재귀함수 호출 문제
문제 출처 : 모두코드 13-3
