입력
첫째 줄에 데이터의 개수 n이 입력된다. ( n <= 1,000 )
둘째 줄에 공백을 기준으로 n개 데이터가 입력된다.

출력
n개의 데이터를 입력의 역순으로 출력한다.

입력 예시   
5
1 3 5 6 8

출력 예시
8 6 5 3 1


#include <stdio.h>

int main()
{
    int aList[1001] = {0,};
    int n, i;
    scanf("%d", &n);
    for(i=0; i<n; i++){
        scanf("%d", &aList[i]);
    }
    for(i=n-1; i>=0; i--){
        printf("%d ", aList[i]);
    }
    
    

    return 0;
}
